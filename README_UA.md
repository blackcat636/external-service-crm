# Content Zavod - Трекер продуктивності мультиплатформ

Веб-додаток для відстеження творців та їх відео продуктивності на Instagram (активно), Telegram (активно) та YouTube (скоро). Побудовано на Next.js 18, TypeScript та Tailwind CSS. Має сучасний темний UI з 3D ефектами та кастомним курсором. Вся бізнес-логіка обробляється через n8n workflows.

> **🇬🇧 English version**: [README.md](./README.md)

## Технологічний стек

- **Frontend**: Next.js 18 (App Router), TypeScript, Tailwind CSS v4
- **UI**: Темна тема, компоненти shadcn/ui, Three.js (3D сфера), Framer Motion
- **Backend**: Зовнішній NestJS сервіс (`crm_external_service`) з інтеграцією SSO
- **База даних**: n8n Data Tables (через зовнішній бекенд)
- **Локальне зберігання**: Транскрипції, унікальний текст та дані генерації відео зберігаються в браузерному localStorage
- **Авторизація**: SSO через основний CRM сервер (JWT service токени)
- **Хостинг**: Self hosted

## 📖 Документація для розробників

### Для AI агентів (нейронок)

Якщо ви AI агент, який допомагає програмісту працювати з проєктом, обов'язково прочитайте:

**Українською:**
1. **[AI_AGENT_RULES.md](./AI_AGENT_RULES.md)** ⚠️ - **ПОЧНІТЬ З ЦЬОГО!** Короткий список правил
2. **[AI_AGENT_GUIDE.md](./AI_AGENT_GUIDE.md)** - Повна інструкція з усіма правилами та прикладами
3. **[AI_AGENT_QUICK_REFERENCE.md](./AI_AGENT_QUICK_REFERENCE.md)** - Швидкий довідник для швидкого початку
4. **[AI_AGENT_CODE_EXAMPLES.md](./AI_AGENT_CODE_EXAMPLES.md)** - Готові приклади коду для копіювання

**English:**
1. **[AI_AGENT_RULES_EN.md](./AI_AGENT_RULES_EN.md)** ⚠️ - **START HERE!** Short list of rules
2. **[AI_AGENT_GUIDE_EN.md](./AI_AGENT_GUIDE_EN.md)** - Full guide with all rules and examples
3. **[AI_AGENT_QUICK_REFERENCE_EN.md](./AI_AGENT_QUICK_REFERENCE_EN.md)** - Quick reference for quick start
4. **[AI_AGENT_CODE_EXAMPLES_EN.md](./AI_AGENT_CODE_EXAMPLES_EN.md)** - Ready-to-use code examples for copying

> **Важливо**: 
> - НЕ змінюйте логіку авторизації (`lib/auth.ts`, `lib/api.ts`, `hooks/useAuth.ts`)
> - Використовуйте існуючі хуки для роботи з даними
> - Створюйте нові компоненти та хуки для нових функцій
> - Дотримуйтесь темної теми та стилізації проєкту

## Початок роботи

### Вимоги

- Node.js 18+
- npm
- n8n інстанс з webhook workflows
- Зовнішній бекенд сервіс (`crm_external_service`) запущений та налаштований
- Основний CRM сервер з підтримкою SSO

### Встановлення

```bash
# Встановити залежності
npm install

# Скопіювати змінні оточення
cp .env.example .env.local

# Відредагувати .env.local з вашою конфігурацією

# Запустити сервер розробки
npm run dev
```

### Змінні оточення

`.env.local`:

```env
# Фронтенд основного сервісу (для SSO initiate)
NEXT_PUBLIC_EXTERNAL_SERVICE_URL=http://localhost:3000

# Бекенд стороннього сервісу (для SSO exchange та API запитів)
NEXT_PUBLIC_EXTERNAL_SERVICE_BACKEND_URL=http://localhost:3001
```

## Структура проєкту

```
contentzavod/
├── app/                          # Next.js App Router
│   ├── (auth)/login/            # Сторінка логіну з 3D ефектом
│   ├── (dashboard)/             # Головний додаток з темним sidebar
│   │   ├── dashboard/           # Сторінка dashboard (огляд мультиплатформ)
│   │   ├── authors/             # Сторінка Instagram Authors
│   │   ├── tracking/            # Сторінка Instagram Tracking
│   │   ├── generate/[videoId]/  # Сторінка генерації відео + Edit with AI
│   │   ├── telegram/            # Сторінки Telegram (активно)
│   │   │   ├── authors/         # Сторінка каналів
│   │   │   ├── tracking/        # Сторінка постів
│   │   │   └── post/[postId]/   # Сторінка редагування поста + Edit with AI
│   │   └── youtube/             # Placeholder сторінки YouTube
│   │       ├── authors/         # Скоро
│   │       └── tracking/        # Скоро
│   └── (auth)/sso-callback/    # Обробник SSO callback
├── components/                   # React компоненти
│   ├── auth/                    # AuthGuard
│   ├── layout/                  # Sidebar (згортається), NavLink, UserProfile
│   ├── shared/                  # Button, LoadingSpinner, PlaceholderPage, EditWithAIDialog
│   ├── dashboard/               # StatCard
│   ├── authors/                 # AddAuthorForm, AuthorCard, AuthorList
│   ├── tracking/                # VideoCard, VideoList
│   ├── telegram/                # TelegramUsernameGuard, ChannelCard, ChannelList, PostCard, PostList
│   ├── custom-cursor.tsx        # Ефект слідування курсора
│   └── sentient-sphere.tsx      # 3D анімована сфера (фон логіну)
├── hooks/                       # Custom React хуки
│   ├── useAuth.ts              # Управління станом SSO авторизації
│   ├── useUserProfile.ts       # Отримання профілю користувача
│   ├── useDashboard.ts         # Отримання даних dashboard
│   ├── useAuthors.ts           # CRUD авторів (додати, оновити через додавання, видалити)
│   ├── useVideos.ts            # Отримання відео + транскрибування/унікальний текст (localStorage)
│   ├── useTelegramChannels.ts  # CRUD каналів Telegram
│   ├── useTelegramPosts.ts     # Пости Telegram + унікальний текст (localStorage)
│   └── useEditWithAI.ts        # Хук редагування тексту з AI
├── lib/                         # Утиліти
│   ├── utils.ts                # cn(), formatNumber, тощо
│   ├── auth.ts                 # Управління SSO токенами (localStorage)
│   ├── telegram-auth.ts        # getTelegramUsername, setTelegramUsername
│   └── api.ts                  # API клієнт для зовнішнього бекенду
└── types/                       # TypeScript визначення
    ├── author.ts
    ├── video.ts
    ├── telegram.ts              # TelegramChannel, TelegramPost, TelegramPostLocalData
    ├── dashboard.ts
    └── api.ts
```

## Сторінки

### Логін (`/login`)

- Темна тема з анімованим 3D сферним фоном (SentientSphere)
- Автоматичне перенаправлення на SSO авторизацію
- Після успішного SSO перенаправляє на dashboard
- Service токен зберігається в localStorage

### Dashboard (`/dashboard`)

- Темна тема з glass-morphism картками
- Огляд мультиплатформ з трьома картками платформ:
  - **Instagram**: Активно, показує кількість відстежуваних акаунтів з фіолетово-рожевим градієнтом
  - **YouTube**: Placeholder "скоро" з червоним градієнтом
  - **Telegram**: Placeholder "скоро" з синьо-бірюзовим градієнтом
- Сітка статистики: загальна кількість підписників, загальна кількість відео, загальна кількість переглядів

### Authors (`/authors`)

- Додавання URL профілів Instagram
- Перегляд відстежуваних авторів з фото профілю та оцінкою вірусного відсотка
- **Сортування за вірусним відсотком** (найбільш вірусні зверху)
- Візуальні ефекти на основі viral_percentage (0-25% сірий, 26-50% синій, 51-75% зелений, 76-100% золотий з анімаціями)
- Оновлення окремих авторів (використовує endpoint додавання автора внутрішньо)
- Видалення авторів
- Нові автори з'являються одразу після додавання (автоматичне оновлення)

### Tracking (`/tracking`)

- Перегляд всіх відстежуваних відео з відео плеєром (підтримує вертикальні 9:16 відео)
- **Сортування за відсотком engagement** (найбільш залучуючі зверху)
- Візуальні ефекти на основі engagement (ті ж рівні, що й для авторів)
- Метрики: перегляди (з fallback N/A для null), лайки, коментарі
- Кнопка Transcribe: конвертує аудіо відео в текст, потім стає кнопкою "View Transcription"
- **Обробка відсутності мови**: Якщо транскрипція повертає "no text in this video", показує повідомлення "No speech detected" та приховує кнопки дій
- Кнопка Make Unique: переписує транскрипцію в унікальну версію (доступна тільки після транскрипції), потім стає кнопкою "View Unique"
- Кнопки View Transcription/Unique відкривають модальні діалоги для перегляду тексту
- Кнопка Generate Video: відкриває нову сторінку для генерації відео (доступна тільки після унікального тексту)
- Дані транскрипції, унікального тексту та генерації відео зберігаються в localStorage (не в базі даних)

### Telegram Channels (`/telegram/authors`)

- **Telegram Username Guard**: Запитує Telegram username при першому відвідуванні (зберігається в localStorage)
- Інструкційний банер: "Запросіть @alexand_content_bot до каналу, щоб почати відстеження"
- Перегляд відстежуваних каналів тільки з назвою (chat_id та username приховані)
- Кнопка видалення для зупинки відстеження каналу
- Канали додаються автоматично, коли бот запрошений до каналу

### Telegram Posts (`/telegram/tracking`)

- **Telegram Username Guard**: Те саме, що й на сторінці каналів
- Перегляд постів з відстежуваних каналів з:
  - Сіткою зображень (підтримує кілька зображень на пост)
  - Підписом з "Read more" для довгого тексту
  - Бейджем назви каналу
- Кнопка Make Unique: переписує підпис в унікальну версію
- Кнопка "Edit Post": відкриває окрему сторінку редагування (тільки після генерації унікального тексту)
- Унікальний текст зберігається в localStorage

### Telegram Post Edit (`/telegram/post/[postId]`)

- Кнопка назад на сторінку постів
- Перегляд оригінального підпису (згортається)
- Редагуване textarea для унікального тексту
- **Кнопка Edit with AI**: Відкриває діалог для надання AI інструкцій
- Кнопки Copy та Save
- Зміни зберігаються в localStorage

### Placeholder сторінки YouTube

- `/youtube/authors`, `/youtube/tracking`
- Анімоване повідомлення "To Be Continued..."
- Градієнтні ефекти, що відповідають кольорам платформи
- Бейджі "Coming soon"

### Generate (`/generate/[videoId]`)

- Показує унікальний текст в редагуваному textarea
- **Кнопка Edit with AI**: Відкриває діалог для надання AI інструкцій для редагування тексту
- **Селектор орієнтації**: Вибір між вертикальним (9:16) або горизонтальним (16:9) форматом відео
- Три опції генерації: VEO3, SORA, Avatar
- **Асинхронний паттерн polling**: Генерація займає ~5 хвилин, тому використовується job-based polling
  - Кнопка Start запускає створення job, повертає одразу з job ID
  - Клієнт опитує статус кожні 10 секунд
  - Показує минулий час під час генерації (наприклад, "2:30")
  - Прогрес зберігається в localStorage - можна вийти та повернутися
  - Таймаут після 15 хвилин
- Показує посилання "View" для вже згенерованих відео
- URL згенерованих відео та pending jobs зберігаються в localStorage

## Потік авторизації (SSO)

### Як це працює

1. **Користувач відкриває додаток** → перевіряє service токен
2. **Якщо токен відсутній/недійсний:**
   - Frontend перенаправляє на `{EXTERNAL_SERVICE_URL}/auth/sso/initiate` (фронтенд основного сервісу)
   - Зовнішній бекенд перенаправляє на `{MAIN_FRONTEND_URL}/sso/initiate?redirect_uri=...&service=contentzavod`
   - Фронтенд основного CRM обробляє SSO логін
3. **Після успішного логіну:**
   - Фронтенд основного CRM перенаправляє на `{CALLBACK_URL}?code={sso_code}` (на `/sso-callback`)
   - Frontend викликає `POST {EXTERNAL_SERVICE_BACKEND_URL}/auth/sso/exchange` (бекенд стороннього сервісу)
   - Зовнішній бекенд обмінює код на service токен (JWT RS256) через основний сервер
   - Service токен зберігається в браузерному localStorage
4. **Наступні запити:**
   - Service токен автоматично додається в заголовок `Authorization: Bearer <token>`
   - Зовнішній бекенд валідує токен та обробляє запити

### Управління токенами

- **Зберігання**: localStorage (ключ: `contentzavod-service-token`)
- **Тип**: JWT RS256, тип `service`
- **Термін дії**: 90 днів
- **Автоматичне оновлення**: При помилці 401 - автоматичне перенаправлення на SSO

## API Endpoints (Зовнішній бекенд)

Всі endpoints знаходяться на зовнішньому бекенді (`EXTERNAL_SERVICE_URL`). Всі запити потребують заголовок `Authorization: Bearer <service-token>`.

### Endpoints авторизації

| Method | Endpoint                      | Description                                    |
| ------ | ----------------------------- | ---------------------------------------------- |
| GET    | `/auth/sso/initiate`          | Ініціювати SSO логін (перенаправляє на основний фронтенд) |
| POST   | `/auth/sso/exchange`          | Обміняти SSO код на service токен            |
| GET    | `/auth/check`                 | Перевірити статус авторизації                     |

### Instagram Endpoints

| Method | Endpoint                                      | Description                                    |
| ------ | --------------------------------------------- | ---------------------------------------------- |
| GET    | `/operations/contentzavod/authors`             | Отримати всіх авторів для користувача                       |
| POST   | `/operations/contentzavod/authors/add`        | Додати нового автора (body: `{ url }`)              |
| POST   | `/operations/contentzavod/authors/delete`      | Видалити автора (body: `{ id }`)                |
| GET    | `/operations/contentzavod/videos`              | Отримати всі відео для користувача                        |
| POST   | `/operations/contentzavod/videos/transcribe`   | Транскрибувати відео (body: `{ videoUrl }`)       |
| POST   | `/operations/contentzavod/videos/unique`       | Зробити текст унікальним (body: `{ text }`)           |
| POST   | `/operations/contentzavod/videos/generate/start` | Почати асинхронну генерацію відео (body: `{ type, videoId, text, orientation }`) |
| GET    | `/operations/contentzavod/videos/generate/status` | Перевірити статус генерації (query: `?jobId=xxx`) |
| GET    | `/operations/contentzavod/dashboard`           | Отримати статистику dashboard                       |

### Telegram Endpoints

| Method | Endpoint                                        | Description                                              |
| ------ | ----------------------------------------------- | -------------------------------------------------------- |
| GET    | `/operations/contentzavod/telegram/channels`    | Отримати всі канали для користувача (потребує X-Telegram-Username) |
| POST   | `/operations/contentzavod/telegram/channels/delete` | Видалити канал (body: `{ id }`)                          |
| GET    | `/operations/contentzavod/telegram/posts`       | Отримати всі пости для користувача (потребує X-Telegram-Username)    |
| POST   | `/operations/contentzavod/telegram/posts/unique` | Зробити текст унікальним (body: `{ text }`)                      |

### Спільні Endpoints

| Method | Endpoint                              | Description                                  |
| ------ | ------------------------------------- | -------------------------------------------- |
| POST   | `/operations/contentzavod/edit-with-ai` | Редагувати текст з AI (body: `{ text, prompt }`) |
| GET    | `/operations/profile`                 | Отримати профіль користувача                             |

## n8n Webhook Contracts

### Add Author

- **Input**: `{ userLogin: string, instagramUrl: string }`
- **Output**: `Author` object
- **Note**: Також використовується для оновлення авторів - оновить існуючого автора, якщо URL збігається

### Get Authors

- **Input**: `{ userLogin: string }`
- **Output**: `Author[]`

### Delete Author

- **Input**: `{ userLogin: string, id: number }`
- **Output**: `{ success: boolean }`

### Get Videos

- **Input**: `{ userLogin: string }`
- **Output**: `Video[]`

### Transcribe Video

- **Input**: `{ userLogin: string, videoUrl: string }`
- **Output**: `{ transcription: string }`

### Unique Text

- **Input**: `{ userLogin: string, text: string }`
- **Output**: `{ unique_text: string }`

### Generate Video (DEPRECATED - Синхронний)

- **Input**: `{ userLogin: string, type: "veo3" | "sora" | "avatar", videoId: string, text: string, orientation: "vertical" | "horizontal" }`
- **Output**: `{ link: string }`
- **Note**: Таймаут на Vercel. Використовуйте асинхронні endpoints нижче замість цього.

### Generate Video Start (Асинхронний)

- **Webhook**: `POST /webhook/generate-video-start-zavod`
- **Input**: `{ userLogin: string, type: "veo3" | "sora" | "avatar", videoId: string, text: string, orientation: "9:16" | "16:9" }`
- **Output**: `{ job_id: string, status: "pending" }`
- **Behavior**:
  1. Створює job в n8n Data Table зі статусом "pending"
  2. Повертає одразу з job_id
  3. Продовжує обробку відео асинхронно
  4. Оновлює статус job на "completed" або "failed" коли готово

### Generate Video Status (Асинхронний)

- **Webhook**: `GET /webhook/generate-video-status-zavod?userLogin=xxx&jobId=xxx`
- **Output**: Повний запис job з n8n Data Table:

```json
{
  "id": 5,
  "status": "pending" | "processing" | "completed" | "failed",
  "link": null | "https://...",
  "error": null | "Error message",
  "createdAt": "2025-12-23T23:22:08.714Z",
  "updatedAt": "2025-12-23T23:25:38.823Z"
}
```

### Get Dashboard

- **Input**: `{ userLogin: string }`
- **Output**: `DashboardStats` object

### Get Telegram Channels

- **Webhook**: `GET /webhook/get-groups-telegram?username=xxx`
- **Output**: `TelegramChannel[]`

```json
[
  {
    "chat_id": "-4920897788",
    "username": "kamidummy",
    "title": "Channel Name",
    "id": 1,
    "createdAt": "2025-12-27T00:16:03.611Z",
    "updatedAt": "2025-12-27T00:16:03.611Z"
  }
]
```

### Delete Telegram Channel

- **Webhook**: `POST /webhook/delete-group-telegram`
- **Input**: `{ id: number }`
- **Output**: Видалений запис каналу

### Get Telegram Posts

- **Webhook**: `GET /webhook/get-the-posts?username=xxx`
- **Output**: `TelegramPost[]`

```json
[
  {
    "chat_id": "-4920897788",
    "caption": "Post text...",
    "media_id": "14134363925757842",
    "media": "[\"https://i.ibb.co/...\",\"https://i.ibb.co/...\"]",
    "channel_name": "Channel Name",
    "id": 1,
    "createdAt": "2025-12-27T00:26:23.137Z",
    "updatedAt": "2025-12-27T00:26:33.379Z"
  }
]
```

### Unique Text (Telegram)

- **Webhook**: `POST /webhook/unique-zavod-telegram`
- **Input**: `{ userLogin: string, username: string, text: string }`
- **Output**: `{ unique_text: string }`

### Edit with AI

- **Webhook**: `POST /webhook/edit-with-ai`
- **Input**: `{ userLogin: string, text: string, prompt: string }`
- **Output**: `{ text: string }`

## Типи даних

### Author

```typescript
interface Author {
  id: number;
  username: string;
  full_name: string;
  profile_picture: string;
  instagram_url: string;
  followers: number;
  viral_percentage: number;
  client_id: string;
  createdAt: string;
  updatedAt: string;
}
```

### Video

```typescript
interface Video {
  id: number;
  url_instagram: string;
  video_url: string;
  profile_url: string;
  username: string;
  views: number;
  likes: number;
  comments: number;
  engagement: string; // відсоток як рядок
  createdAt: string;
  updatedAt: string;
}

// Зберігається в localStorage для кожного ID відео
interface VideoLocalData {
  transcribed_text?: string;
  unique_text?: string;
  sora_video?: string;
  veo3_video?: string;
  avatar_video?: string;
}
```

### TelegramChannel

```typescript
interface TelegramChannel {
  id: number;
  chat_id: string; // Приховано від UI
  username: string; // Приховано від UI
  title: string; // Відображається
  createdAt: string;
  updatedAt: string;
}
```

### TelegramPost

```typescript
interface TelegramPost {
  id: number;
  chat_id: string;
  caption: string;
  media_id: string;
  media: string; // JSON рядок URL зображень - потрібен JSON.parse()
  channel_name: string;
  createdAt: string;
  updatedAt: string;
}

// Зберігається в localStorage для кожного ID поста
interface TelegramPostLocalData {
  unique_text?: string;
  original_caption?: string;
}
```

### DashboardStats

```typescript
interface DashboardStats {
  authors: string;
  followers: number;
  number_of_videos: number;
  total_views: number;
}
```

## Візуальні ефекти

### UI Тема

- Темний фон (#050505 до #0f0f0f)
- Glass-morphism картки (білі/5 до білих/10 фони)
- Синій акцентний колір (#2563eb)
- Моноширинні шрифти для міток та даних
- Анімована 3D сфера на сторінці логіну
- **Кастомний курсор**: Ефект слідування курсора з mix-blend-difference, розширюється при наведенні на інтерактивні елементи

### Навігація Sidebar

- **Згортаючі секції платформ**: Instagram, Telegram, YouTube
- Instagram: Authors, Tracking (активно)
- Telegram: Channels, Posts (активно)
- YouTube: Authors, Tracking (скоро)
- Стан розгортання/згортання зберігається в localStorage
- Іконки платформ з градієнтними фонами
- Бейджі "Soon" для майбутніх платформ (тільки YouTube)

### Рівні Engagement/Viral відсотка

| Діапазон   | Стиль бейджа     | Стиль картки  | Ефекти                   |
| ------- | --------------- | ----------- | ------------------------- |
| 0-25%   | Сірий            | Базовий       | Немає                      |
| 26-50%  | Синій            | Синій border | Немає                      |
| 51-75%  | Зелений з світінням | Зелений ring  | Світіння при наведенні             |
| 76-100% | Золотий градієнт   | Помаранчевий ring | Пульсуюча анімація, іскорки |

## Примітки для розробки

### Авторизація

- SSO через основний CRM сервер
- Service токен (JWT RS256) зберігається в localStorage
- Автоматичне перенаправлення на SSO при помилках 401
- Токен автоматично додається до всіх API запитів

### Логіка оновлення автора

- Оновлення тепер повторно використовує endpoint додавання автора з тим самим Instagram URL
- n8n обробляє оновлення існуючих авторів, коли URL вже існує
- Окремий webhook оновлення не потрібен

### Потік даних

1. Користувач відкриває додаток → перевіряє service токен
2. Якщо токен відсутній → SSO потік
3. Після SSO → service токен зберігається в localStorage
4. Frontend робить запити до зовнішнього бекенду з токеном
5. Зовнішній бекенд валідує токен та викликає n8n webhooks
6. n8n повертає дані для користувача
7. Frontend відображає дані

### Ключі Local Storage

| Ключ                         | Призначення                                                      |
| --------------------------- | ------------------------------------------------------------ |
| `contentzavod-service-token` | Service токен (JWT) для авторизації                       |
| `telegram-username`         | Telegram username для API запитів                           |
| `video_local_data`          | Транскрипції відео Instagram, унікальний текст, згенеровані відео |
| `telegram_post_local_data`  | Унікальний текст постів Telegram                                    |
| `sidebar_expanded_sections` | Стан розгортання sidebar                                      |

### Local Storage для даних відео

- Транскрипції, унікальний текст та URL згенерованих відео зберігаються в localStorage
- Ключ: `video_local_data`
- Структура: `{ [videoId]: VideoLocalData }`
- Дані зберігаються в браузері, не синхронізуються з базою даних

### Local Storage для постів Telegram

- Унікальний текст та оригінальний підпис зберігаються в localStorage
- Ключ: `telegram_post_local_data`
- Структура: `{ [postId]: TelegramPostLocalData }`
- Дані зберігаються в браузері, не синхронізуються з базою даних

### Стилізація

- Tailwind CSS v4 з кастомними змінними темної теми
- Адаптивний дизайн (mobile-first)
- Glass-morphism та blur ефекти
- Послідовні паттерни компонентів з темними фонами

## Команди

```bash
npm run dev      # Запустити сервер розробки
npm run build    # Зібрати для продакшну
npm run start    # Запустити продакшн сервер
npm run lint     # Запустити ESLint
```

## Майбутні покращення

- [ ] Замінити n8n Data Tables на Supabase
- [ ] Реалізувати Supabase Auth
- [x] Додати відображення транскрипції відео
- [x] Додати управління переписаним текстом (унікальний текст)
- [x] Видалення авторів
- [x] Додати тригер генерації AI відео (інтеграція webhook VEO3, SORA, Avatar)
- [x] Селектор орієнтації відео (вертикальна/горизонтальна)
- [x] Сучасний темний UI редизайн
- [x] Кастомний курсор / ефект слідування курсора
- [x] Мультиплатформний sidebar (Instagram, YouTube, Telegram)
- [x] Placeholder сторінки YouTube та Telegram
- [x] Автори відсортовані за вірусним відсотком
- [x] Відео відсортовані за engagement
- [x] Обробка "No speech detected" для відео без аудіо
- [x] Автоматичне оновлення після додавання нового автора
- [ ] Інтеграція YouTube (API + відстеження)
- [x] Інтеграція Telegram (API + відстеження)
- [x] Управління каналами Telegram (перегляд, видалення)
- [x] Відстеження постів Telegram з сітками зображень
- [x] Функція Edit with AI (Instagram + Telegram)
- [x] Заголовки безпеки webhook (X-N8N-Secret)
- [ ] Реалізувати фільтрацію та пошук
- [ ] Додати пагінацію
- [ ] Створити аналітичні графіки
- [ ] Підтримка командних акаунтів
